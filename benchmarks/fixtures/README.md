# Phase 1 regression fixtures

These manifests pin the local reference and reconstruction artifacts used when
the architecture was branched. The binary artifacts stay in the existing
local work directories; SHA-256 values prevent silently switching inputs.

The Eiffel fixture intentionally records the current conversion failure. That
is the baseline we will replace in the later structure phase, while Phase 1
only moves detection and routing behind explicit data contracts.
